ROLE: You are a senior full-stack developer working in my existing project. Do not rewrite what is already there; add to it.

GOAL: My screen currently shows the typical 4-room resale price in ANG MO KIO as a hard-coded value. Replace it with real data from the HDB resale flat prices dataset on data.gov.sg, fetched through a serverless function of my own. The user must be able to pick ANY HDB town in Singapore and ANY of these flat types: 2 ROOM, 3 ROOM, 4 ROOM, MULTI-GENERATION. Default selection on first load stays ANG MO KIO + 4 ROOM, so the screen looks the same as today until the user changes it.

Allowed values (use these exact strings; they are what the dataset stores, in upper case):

town: ANG MO KIO, BEDOK, BISHAN, BUKIT BATOK, BUKIT MERAH, BUKIT PANJANG, BUKIT TIMAH, CENTRAL AREA, CHOA CHU KANG, CLEMENTI, GEYLANG, HOUGANG, JURONG EAST, JURONG WEST, KALLANG/WHAMPOA, MARINE PARADE, PASIR RIS, PUNGGOL, QUEENSTOWN, SEMBAWANG, SENGKANG, SERANGOON, TAMPINES, TOA PAYOH, WOODLANDS, YISHUN

flat_type: 2 ROOM, 3 ROOM, 4 ROOM, MULTI-GENERATION (note the hyphen)
Keep both lists in ONE shared constants file so the screen's dropdowns and the api/ validation use the same source.

api/resale.js — accepts ?town=...&flat_type=... as query parameters. Reject any value not in the allowed lists with 400 and a one-line reason, BEFORE calling the upstream; never pass user input into the filter unchecked. Then call:
https://data.gov.sg/api/action/datastore_search?resource_id=d_8b84c4ee58e3cfc0ece0d773c8ca6abc&filters={"town":"<TOWN>","flat_type":"<FLAT_TYPE>"}&sort=month desc&limit=1000
Build the filters object with JSON.stringify and pass the whole query through URLSearchParams (or encodeURIComponent) so braces, quotes, spaces and the "/" in KALLANG/WHAMPOA are encoded correctly. Return only the fields my screen needs and nothing else: town, flat_type, the latest month found, the number of transactions used, and the typical price (define "typical" as the MEDIAN resale_price of the latest 3 months present in the result, so rare types like MULTI-GENERATION still get a figure).

api/health.js — reports whether the credential is configured (keyConfigured) and whether the upstream answered, including the HTTP status it returned. It must never print the credential or any part of it.

On the screen, add two dropdowns (town, flat type) filled from the shared constants, and replace the hard-coded value with the live one for the current selection. Changing either dropdown triggers a new call to /api/resale. Decide what the user sees in each of these four cases: the data is loading, the data is empty (e.g. no MULTI-GENERATION sales in that town), the upstream refused, and the upstream is unreachable. I want four different sentences, not one spinner, and the empty sentence must name the town and flat type the user picked. If the user changes selection while a request is in flight, ignore the older response (AbortController or a request id) so a slow reply never overwrites a newer one.

OUTPUT: Both functions at api/ in the PROJECT ROOT, siblings of package.json, never inside src/. If this project has a server entry file, register the same two routes there too, because that is the shape the preview can answer. If it has no server file, skip that and tell me so rather than inventing one. Make sure package.json contains "type": "module". BEFORE the fetch, if the credential is missing or empty, return 503 with a message naming the variable, and do not call the upstream at all. A missing variable is sent as the word "undefined" and looks exactly like a wrong credential, so stop it early. AFTER the fetch, check response.ok before reading the body. A refusal often has an empty body, so calling .json() on it throws and my function dies with a 500 instead of telling me what happened. On a non-2xx reply, return the upstream status and a one-line reason in your own JSON. Cache the response for one day with Cache-Control: s-maxage=86400, stale-while-revalidate=172800, matching how often the source actually changes. Because the town and flat type are in the query string, each combination is cached separately; do not strip the query string from the cache key. In the footer, credit the source in the exact form the provider's licence asks for.

GUARDRAILS: Never write the credential into any file, comment or README. Never create a variable whose name starts with VITE_. Never call the upstream from browser code; every call happens inside api/, and the browser only ever calls /api/resale. Never print the credential, or any part of it, in a response or a log. Never forward arbitrary query parameters to the upstream; only the validated town and flat_type. Do not fetch all towns at once on page load; one upstream call per selected combination. No new npm packages. No database, no login. Leave every screen I already have working exactly as it is.

CONTEXT: Deployed on Vercel from GitHub. The credential lives only in a Vercel environment variable named none: this dataset is open and needs no credential, so keyConfigured is the literal string "not required", no environment variable is read, and the 503 guard above does not apply. Also note that resale_price arrives as a string despite being declared numeric (convert with Number() and drop anything that isn't finite), and that the datastore returns the oldest records first unless the sort parameter is honoured, so check the month on the first record before rendering anything as current; if the first record is not the newest month in the result, sort the records yourself by month descending before computing the median. A real response from the endpoint, called by hand just now, looks like this: {"success":true,"result":{ "resource_id":"d_8b84c4ee58e3cfc0ece0d773c8ca6abc", "fields":[{"type":"text","id":"month"},{"type":"text","id":"town"}, {"type":"text","id":"flat_type"},{"type":"text","id":"block"}, {"type":"text","id":"street_name"},{"type":"text","id":"storey_range"}, {"type":"text","id":"floor_area_sqm"},{"type":"text","id":"flat_model"}, {"type":"text","id":"lease_commence_date"},{"type":"text","id":"remaining_lease"}, {"type":"numeric","id":"resale_price"},{"type":"int4","id":"_id"}], "records":[ {"_id":34,"month":"2017-01","town":"ANG MO KIO","flat_type":"4 ROOM", "block":"472","street_name":"ANG MO KIO AVE 10","storey_range":"10 TO 12", "floor_area_sqm":"92","flat_model":"New Generation", "lease_commence_date":"1979","remaining_lease":"61 years 06 months", "resale_price":"400000"}, {"_id":43,"month":"2017-01","town":"ANG MO KIO","flat_type":"4 ROOM", "block":"207","street_name":"ANG MO KIO AVE 1","storey_range":"04 TO 06", "floor_area_sqm":"97","flat_model":"New Generation", "lease_commence_date":"1976","remaining_lease":"58 years 06 months", "resale_price":"460000"}, {"_id":47,"month":"2017-01","town":"ANG MO KIO","flat_type":"4 ROOM", "block":"588C","street_name":"ANG MO KIO ST 52","storey_range":"13 TO 15", "floor_area_sqm":"90","flat_model":"DBSS","lease_commence_date":"2011", "remaining_lease":"93 years 08 months","resale_price":"688000"}], "_links":{"start":"/api/action/datastore_search?resource_id=d_8b84c4ee58e3cfc0ece0d773c8ca6abc", "next":"/api/action/datastore_search?resource_id=d_8b84c4ee58e3cfc0ece0d773c8ca6abc&offset=100"}, "total":240345}}
